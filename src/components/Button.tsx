const styles = {
    padding: 10,
    borderRadius:5,
    fontWeight:600

}

function Button() {
    return (
        <button style={styles}>
            Login
        </button>);
};

export default Button;